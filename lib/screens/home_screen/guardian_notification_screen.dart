import 'package:flutter/material.dart';

class GuardianNotificationScreen extends StatelessWidget {
  const GuardianNotificationScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("Guardian Notification Screen"),
      ),
    );
  }
}
