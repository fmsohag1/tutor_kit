import 'package:flutter/material.dart';

class TeacherOfferScreen extends StatelessWidget {
  const TeacherOfferScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("Teacher Offers Screen")
      ),
    );
  }
}
