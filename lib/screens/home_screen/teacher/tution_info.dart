import 'package:flutter/material.dart';

class TutionInfo extends StatelessWidget {
  const TutionInfo({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("Tution Info page"),
      ),
    );
  }
}
